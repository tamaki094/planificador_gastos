import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'app-ordenes-compra',
  imports: [],
  templateUrl: './OrdenesCompra.html',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export default class OrdenesCompra { }
